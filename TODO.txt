- figure out the data structure for reconciling no User but with messages.
- write a function that will remove duplicates in the users model.
- what data needed to be refreshed? messages? users? can i only refresh messages?
- namespacing for the socket.io (only send data to the 'to' client)
- manually update the front end when user sends text messages.
- when receive websocket request and it is not showing, then show a notification.
- when 'ADD NEW USER" adds weird shit to the front end.